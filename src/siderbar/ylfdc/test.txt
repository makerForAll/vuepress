IF(
  AND({当前序状态} == "序/已完成",({截止已过_天数} > 0)), 
  IF(
    ({小帐_性质} == "实收"), 
    ({小账金额} * -1), 
    IF(
        ({小帐_性质} == "实收资源"), 
        ({小账金额} * -1), 
        IF(
            ({小帐_性质} == "押金抵扣"), 
            ({小账金额} * -1), 
            IF(
                ({小帐_性质} == "中途减免"),
                ({小账金额} * -1), 
                IF({小帐_性质} == "转出", ({小账金额} * 1),0)
              )
           )
     )),
   IF(
        AND(
            ({小帐_性质} == "应缴"), 
            ({截止已过_天数} > 0)
            ), 
        {小账金额}, 0
      )
    )
